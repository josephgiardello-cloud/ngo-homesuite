# Optional cloud provider dependencies
boto3==1.42.18
botocore==1.43.9
azure-identity==1.25.1
azure-keyvault-keys==4.11.1

# Payment processing (optional — required for Stripe Checkout and webhook handling)
stripe>=8.0.0
